const fs=require('fs'); const s=fs.readFileSync('index.html','utf8'); const tagRe=/<(\/)?([a-zA-Z0-9-]+)([^>]*)>/g; const voids=new Set(['area','base','br','col','embed','hr','img','input','link','meta','param','source','track','wbr']); let m; const stack=[]; let lineOffsets=[0]; s.split('\n').forEach((l,i)=>lineOffsets[i+1]=lineOffsets[i]+l.length+1); while((m=tagRe.exec(s))){ const isClose=!!m[1]; const tag=m[2].toLowerCase(); const rest=m[3]; const pos=m.index; const line=lineOffsets.findIndex(off=>off>pos)-1; if(isClose){ if(stack.length===0){ console.log('Unmatched closing',tag,'at',line+1); } else { const top=stack[stack.length-1]; if(top.tag===tag){ stack.pop(); } else { console.log('MISMATCH at line',line+1,'closing',tag,'but top is',top.tag); console.log('Stack snapshot (top last):'); stack.slice(-10).forEach((e,i,arr)=>console.log((arr.length- i)+':',e.tag,'opened at',e.line)); console.log('---'); } } } else { const selfClosing = /\/$/.test(rest) || voids.has(tag); if(!selfClosing){ stack.push({tag,line:line+1}); } } } if(stack.length){ console.log('Unclosed tags at EOF:'); stack.forEach(e=>console.log(e.tag,'opened at',e.line)); } else console.log('All tags matched');