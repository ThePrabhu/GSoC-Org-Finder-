const fs=require('fs'); const s=fs.readFileSync('index.html','utf8'); const start=s.indexOf('<section id="watchlist"'); if(start===-1){console.error('start not found'); process.exit(2);} const end=s.indexOf('</section>', start); const snippet=s.slice(start, end+10); const opens=(snippet.match(/<div(\s|>)/g)||[]).length; const closes=(snippet.match(/<\/div>/g)||[]).length; console.log('div opens',opens,'div closes',closes,'difference',opens-closes);